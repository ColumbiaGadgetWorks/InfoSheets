[project]
name = "is2"
version = "0.1.0"
description = "CGW Information Sheets"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mkdocs>=1.6.1",
    "mkdocs-material>=9.6.20",
    "qrcode[pil]",
    "mkdocs-macros-plugin",
]
